import React, { useState, useEffect } from 'react';

const Convert = ({ text, lang }) => {
    useEffect(() => {
        
    }, [text, lang])

    return (
        <div></div>
    );
};

export default Convert;